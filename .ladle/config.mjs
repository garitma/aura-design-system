export default {
  outDir: "ladle-build/dist",
  addons: {
    theme: {
      enabled: false,
      defaultState: "light",
    },
    rtl: {
      enabled: false,
      defaultState: true,
    }
  },
};
