---
title: Alert Dialog
description: A modal dialog that interrupts the user with important content and expects a response.
---

## Installation

```bash
pnpm dlx shadcn@latest @aura/alert-dialog
```
## Usage

### Default

```tsx
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "@/components/ui/AlertDialog";
import { Button } from "@/components/ui/Button";

export const Default = () => (
  <AlertDialog>
    <AlertDialogTrigger asChild>
      <Button variant="outline">Show Dialog</Button>
    </AlertDialogTrigger>
    <AlertDialogContent>
      <AlertDialogHeader>
        <AlertDialogTitle>Are you absolutely sure?</AlertDialogTitle>
        <AlertDialogDescription>
          This action cannot be undone. This will permanently delete your
          account and remove your data from our servers.
        </AlertDialogDescription>
      </AlertDialogHeader>
      <AlertDialogFooter>
        <AlertDialogCancel asChild>
          <Button variant="outline">Cancel</Button>
        </AlertDialogCancel>
        <AlertDialogAction asChild>
          <Button>Continue</Button>
        </AlertDialogAction>
      </AlertDialogFooter>
    </AlertDialogContent>
  </AlertDialog>
);
```

## Props

### AlertDialog

All props are passed through to the underlying component. See the [Radix UI documentation](https://www.radix-ui.com/primitives) for the full API.

| Prop | Type | Default |
|------|------|---------|
| *All props from* | `React.ComponentProps<typeof AlertDialogRadix.Root>` | - |

### AlertDialogPortal

All props are passed through to the underlying component. See the [Radix UI documentation](https://www.radix-ui.com/primitives) for the full API.

| Prop | Type | Default |
|------|------|---------|
| *All props from* | `React.ComponentProps<typeof AlertDialogRadix.Portal>` | - |

### AlertDialogOverlay

All props are passed through to the underlying component. See the [Radix UI documentation](https://www.radix-ui.com/primitives) for the full API.

| Prop | Type | Default |
|------|------|---------|
| *All props from* | `React.ComponentProps<typeof AlertDialogRadix.Overlay>` | - |

### AlertDialogTrigger

All props are passed through to the underlying component. See the [Radix UI documentation](https://www.radix-ui.com/primitives) for the full API.

| Prop | Type | Default |
|------|------|---------|
| *All props from* | `React.ComponentProps<typeof AlertDialogRadix.Trigger>` | - |

### AlertDialogContent

All props are passed through to the underlying component. See the [Radix UI documentation](https://www.radix-ui.com/primitives) for the full API.

| Prop | Type | Default |
|------|------|---------|
| *All props from* | `React.ComponentProps<typeof AlertDialogRadix.Content>` | - |

### AlertDialogHeader

All props are passed through to the underlying component. See the [Radix UI documentation](https://www.radix-ui.com/primitives) for the full API.

| Prop | Type | Default |
|------|------|---------|
| *All props from* | `React.ComponentProps<"div">` | - |

### AlertDialogFooter

All props are passed through to the underlying component. See the [Radix UI documentation](https://www.radix-ui.com/primitives) for the full API.

| Prop | Type | Default |
|------|------|---------|
| *All props from* | `React.ComponentProps<"div">` | - |

### AlertDialogTitle

All props are passed through to the underlying component. See the [Radix UI documentation](https://www.radix-ui.com/primitives) for the full API.

| Prop | Type | Default |
|------|------|---------|
| *All props from* | `React.ComponentProps<typeof AlertDialogRadix.Title>` | - |

### AlertDialogDescription

All props are passed through to the underlying component. See the [Radix UI documentation](https://www.radix-ui.com/primitives) for the full API.

| Prop | Type | Default |
|------|------|---------|
| *All props from* | `React.ComponentProps<typeof AlertDialogRadix.Description>` | - |

### AlertDialogAction

All props are passed through to the underlying component. See the [Radix UI documentation](https://www.radix-ui.com/primitives) for the full API.

| Prop | Type | Default |
|------|------|---------|
| *All props from* | `React.ComponentProps<typeof AlertDialogRadix.Action>` | - |

### AlertDialogCancel

All props are passed through to the underlying component. See the [Radix UI documentation](https://www.radix-ui.com/primitives) for the full API.

| Prop | Type | Default |
|------|------|---------|
| *All props from* | `React.ComponentProps<typeof AlertDialogRadix.Cancel>` | - |

