// Copyright (c) 2020, garitma.com.
// Buttons
// Licensed under MIT - see LICENSE.txt 
// Aura Design System

.interactive
    min-width 48px
    min-height 48px 

.button
    -webkit-writing-mode: horizontal-tb !important
    box-sizing: border-box
    margin: 0em
    letter-spacing: normal
    word-spacing: normal
    text-transform: none
    text-indent: 0px
    text-shadow: none
    font: inherit
    mixin-align-items(center)
    mixin-border-radius(aura)
    mixin-transition(300ms, background, cubic-bezier(0.4, 0, 0.6, 1) linear)
    font-weight: 600
    display inline-flex
    height (aura * 2) * 2
    padding 0 (aura * 2)
    border 2px solid

.button-fill
    @extend .button
    border-color var(--aura-accents-primary)
    color Dark
    background-color var(--aura-accents-primary)
    &:hover:not(.disable)
        color var(--aura-accents-primary)
        background-color var(--aura-dark)

.button-pill
    @extend .button
    color var(--aura-accents-primary)
    border-color var(--aura-accents-primary)
    background-color transparent
    &:hover:not(.disable)
        background-color var(--aura-accents-primary)
        color var(--aura-dark)

.button-link
    @extend .button
    color: var(--aura-root)
    border: 0
    text-decoration: underline
    background-color transparent
    &:hover:not(.disable)
        color: var(--aura-base-color)
        
a
    text-decoration: none
    color var(--aura-root)
    cursor pointer
    &:visited:not(.button):not(.button-pill):not(.button-link):not(.disable-button):not(.button-fill)
        color var(--aura-root)
    
button
    cursor pointer 
