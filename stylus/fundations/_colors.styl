// Copyright (c) 2021, garitma.com.
// Colors Configuration
// Licensed under MIT - see LICENSE.txt 
// Aura Design System

.white-text
    color var(--aura-white)!important

.black-text
    color var(--aura-black)!important

// Background Colors

colors = "snow" "purple" "white" "black" "orange" "orange-rose" "pink" "blue" "teal-green" "green" "lemon-green" "yellow" "cold-grey" "accents-0" "accents-1" "accents-2" "accents-3" "accents-4"

for color in colors
  .{color}
    background-color: var(replace("color", color, --aura-color))

// Status

status = "warning" "info" "danger" "success"

for state in status
  .{state}
    background-color: var(replace("state", state, --aura-accents-state-bg))!important
  .{state}-text
    color: var(replace("state", state, --aura-accents-state))!important