// Copyright (c) 2021, garitma.com.
// Typo
// Licensed under MIT - see LICENSE.txt 
// Aura Design System

// General

html
    font-family var(--aura-font-stack) 
    line-height 1.21053
    -webkit-font-smoothing: antialiased;
    text-size-adjust: 100%;
    text-rendering: optimizelegibility;
    box-sizing: border-box;

pre
    font-family var(--aura-font-stack)

body
    mixin-visual-scale(visualScale-base, 1, 1)


// Headlines

h1,h2, h3, h4, h5, h6, p, .h1, .h2, .h3, .h4, .h5, .h6 {
  font-size: clamp(var(--min), var(--val), var(--max));
}

h1 .h1 {
  --min: 1em; // minimum value
  --val: 2.5vw; // preferred value = 2.5% viewport width
  --max: visualScale-h1; // maximum value
}

// Content

p
    margin: aura 0

blockquote
    font-family var(--aura-font-quotes)
    text-transform uppercase
    margin 0
    font-weight 600
    @extend .h4

ul
    margin 0
    padding 0
    list-style none

// Weight

.light
    font-weight 200 !important

.regular 
  font-weight: 400 !important;

.bold 
  font-weight: 700 !important;

